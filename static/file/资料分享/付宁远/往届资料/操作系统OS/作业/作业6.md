# 第六次作业

1. FCB分解，当前目录、磁盘碎片整理，开高速缓存、磁盘调度、提前读取、合理分配磁盘空间、信息优化分布、RAID技术

2. 文件的基本信息（包括文件名、物理位置、文件的逻辑结构和物理结构）、文件的访问控制信息（文件所有者以及访问权限）、文件的使用信息（包括创建时间，上一次修改时间，当前使用信息等） 

3. IO系统引入缓冲是为了缓和IO设备与CPU之间速度不匹配的问题

   单缓冲区：(100+50)*10+50=1550μs

   双缓冲区：100&10+50+50=1100μs

4. 先来先服务：5+25+15+50+68+1+35=199, 时间1194ms

   最短寻道时间优先：5+10+7+1+33+3+32=91,时间546ms

   SCAN扫描：70+83=153,时间918ms

   LOOK扫描：55+68=123,时间738ms

5. (1)一个文件系统数据块大小为2^10^，一个目录项为2^7，因此一个磁盘可以存8个目录项，128/8=16，因此找到目录项平均访问（16+1)/2=8.5次磁盘

   文件需要100个磁盘，因此访问一个块需要（1+100）/2=50.5次，总共59次

   (2)一个目录项占14+2=16个字节，所以一个磁盘块存1024/16=64个目录项。

   首先从根节点开始读取根目录下的三个目录项，根节点已经读入内存。

   读取usr2的inode需要1次读取;

   读取usr2的内容需要((128/64)+1)/2=1.5次读取；

   读第三级目录的inode需要1次读取；

   由于没有说第三级目录下的文件数，默认读1次；

   读文件的inode需要读取1次；

   读文件由于采用直接索引，所以直接读1次；

   平均需要6.5次

   （3）管理文件需要数据块数为16*2^70^/2^10^=2^64，因此需要64位，即8字节来表示磁盘块，所以索引区可以放512/8=64个磁盘块号，所以在支持一级索引的情况下，最大文件大小为64 * 2^10/8 * 2^10 = 8MB

   

